@model BooksApp.Models.BookModel

@{
    ViewData["Title"] = "Återlämna";
}

<h1>Återlämna</h1>

<h4>@Model.Title</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AcceptReturn">
            
            <input type="hidden" asp-for="Id" />
           
            <div class="form-group">
                <input type="submit" value="Återlämna bok" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
